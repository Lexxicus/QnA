$('.answer-errors').html("<%= render 'shared/errors', resource: @answer %>");
<% !if @answer.errors.present? %>
  $('.answers').html('<%= j render 'answers/answers', best_answer: @best_answer, other_answers: @other_answers %>');
<%end%>
